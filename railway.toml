[build]
builder = "nixpacks"
buildCommand = "pip install -r backend/requirements.txt"

[deploy]
startCommand = "sh -c 'find / -name main.py -maxdepth 4 2>/dev/null; echo PWD=$(pwd); ls -la; ls backend/ 2>/dev/null; sleep 30'"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
