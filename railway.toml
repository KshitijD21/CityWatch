[build]
builder = "nixpacks"
buildCommand = "pip install -r backend/requirements.txt"

[deploy]
startCommand = "sh -c 'exec uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000} --app-dir backend'"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
